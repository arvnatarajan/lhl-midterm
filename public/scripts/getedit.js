/*$(document).ready(function(){
  $.ajax({
    method:"GET",
    url:"/api/lists"
  }).done(function(points){
    lists.forEach((list) => {
      $('.for-edits').append(`
        <li>${list.name}</li>
       `)
    })
  })
});
*/
